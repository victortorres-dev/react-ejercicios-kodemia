react solo actializa los componentes que hayan camiado
jsx extension de sintaxis

npm startr: ejecuta react scrip start

co para en el vdom y solo regresa los elementos que cambiaron

un componente recibe props y retorna react eleents mediante jsx

el state manipula la logica

functional o statles components y class components

los props son elementos externos al componentes que nos ayudan a setear 
los props son de solo lectura

es functional o statles component , es por que no tiene logica 
solo le estas pasando infformacion

el state se define en el metodo constructor de la clase

// antes el componente como funcion no permitia el state por eso se usaban class component, no pueden heraedar ni tener estados


